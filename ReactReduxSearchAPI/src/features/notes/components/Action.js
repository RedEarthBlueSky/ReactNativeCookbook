import React from 'react';
import { View, Text } from 'react-native';

const Action = (props) => {
  return (
    <View>
      <Text>
        This is the Action component child of Notes
      </Text>
    </View>
  );
};

export default Action;
